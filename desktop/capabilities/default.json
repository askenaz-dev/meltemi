{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal deny-by-default surface: the webview may only use core window/event/webview IPC. Notifications are allowed because the surface must be able to say a permission is waiting while it is behind another window; no fs, no shell, no network plugins — the Rust side owns the daemon connection.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
